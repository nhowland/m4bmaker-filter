"""UI for the offensive-speech filtering feature (G5).

Kept as a separate subpackage from ``m4bmaker/gui/``'s existing files —
same additive-only boundary ``m4bmaker/filter/`` (the backend) has kept
since ADR-0004, now extended to the UI layer as that ADR anticipated.
"""

from __future__ import annotations
